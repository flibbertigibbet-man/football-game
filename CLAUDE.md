# ⚽ 足球球员竞猜

## 线上地址
- **生产环境**：https://football-game-production-c873.up.railway.app
- **部署平台**：Railway（免费版，15分钟无访问休眠）
- **本地启动**：`node server/server.js` → http://localhost:8080

## 技术架构
- 前端：原生 HTML/CSS/JS，无框架
- 后端：Node.js + WebSocket (ws)，JSON 文件数据库（零原生依赖）
- 数据：3722 名球员，`js/data.js`（同步加载） + `data/players.json`（备用）

## 已完成（2026-08-08 ~ 08-09）

### 代码修复
- [x] 隐藏在线对战入口 → 已恢复（服务器部署后重新打开）
- [x] 删除 `leaveOnlineLobby()` 重复定义
- [x] 服务器数据加载从 `eval()` 改为 `JSON.parse()`
- [x] PORT 环境变量支持
- [x] 所有 `ws.send()` 包装为 `safeSend()`（异常保护）
- [x] WebSocket URL 自动适配 wss://
- [x] 重连改成指数退避（2s→4s→8s→…→30s）
- [x] 数据库从 SQLite 换成 JSON 文件（`game-data.json`）

### 部署
- [x] Git 仓库初始化 + 提交
- [x] Railway 部署成功，拿到公开链接
- [x] 单人模式 + 在线对战均可通过公开链接访问

### 文档
- [x] `TESTING.md` 测试手册（58 项测试用例，7 个阶段）

## 明天计划（2026-08-10）：降低新手流失

### 核心问题
用户反馈：游戏太硬核，几次猜不对就不愿意玩了。核心用户是资深球迷，但边缘用户（泛体育人群）流失严重。

### 任务 1：新手引导（预计 30 分钟）
- 首次进入游戏时，强制走一局"教程关"
- 目标固定为梅西或 C 罗（全球认知度最高）
- 引导用户输入→提交→看颜色反馈→理解规则
- 教程结束后弹窗："你已经掌握了规则！去挑战正式模式吧"
- 用 `localStorage` 标记"已完成教程"，老玩家不重复提示

### 任务 2：提示系统（预计 30 分钟）
- 简单难度（难度 1-2）：第 3 次猜错后，自动给一条文字提示
- 提示内容示例：
  - "他拿过欧冠冠军"（ucl ≥ 1）
  - "他是南美球员"（continent === '南美洲'）
  - "他在英超踢球"（league === '英超'）
  - "他不到 25 岁"（age ≤ 25）
- 提示显示在输入框上方，一次只显示一条
- 后续每再猜错 2 次，多给一条提示

### 任务 3：赛后回顾（预计 20 分钟）
- 猜对/猜错弹窗里，增加"你离目标有多远"的视觉展示
- 用之前的猜测记录，标注最接近的一次
- 文案优化："就差一点！你猜的 XX 和目标球员同属 XX 联赛"
- 增加"再来一局"的诱惑力——不靠"再来一局"按钮，靠"差点就中了"的心理

### 任务 4：难度描述优化（预计 10 分钟）
- 当前难度描述过于强调"球迷等级"（资深球迷/球探专家）
- 改为强调"提示多少"和"球员知名度"
- 新描述示例：
  - 1 级：🍼 "全是超级巨星，3次猜错后给提示"
  - 2 级：🎯 "知名球星为主，3次猜错后给提示"
  - 3 级：⚡ "包含部分冷门球员，不给提示"
  - 4 级：🔥 "大量冷门球员，不给提示"
  - 5 级：💀 "全部球员都可能，无任何帮助"

## 产品洞察（讨论记录）

### 用户画像推测
- 核心用户：25-40岁男性，熟悉五大联赛
- 边缘用户：18-25岁泛体育人群，只认识超巨
- 流失用户：觉得太难，3次猜不中就走了（最大潜在用户群）

### 足球 vs 篮球
- 足球情怀深但圈子窄，篮球群众基础大得多
- 猜谜游戏的实际用户不是"球迷"，而是"喜欢猜谜+对体育有一点兴趣的人"
- 意味着实际目标用户比纯粹球迷群体要大

### 变现路径（暂不执行，供参考）
- Wordle 模式：被大平台收购
- 小游戏模式：猜完一局插广告
- 每日限量模式：免费 3 局/天，付费无限
- 品牌合作：跟懂球帝/直播吧合作推广

## 原则
- 不引入框架和构建工具，保持简单
- 不删功能，隐藏即可，将来可以恢复
- 零成本部署优先
- 先验证有人玩，再考虑做大
- **当前优先级：降低新手门槛 > 加功能 > 变现**
