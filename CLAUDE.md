# ⚽ 足球球员竞猜 - 试试水计划

## 目标
把现有人模式部署到免费静态托管平台（GitHub Pages / Vercel），拿到链接分享给朋友玩。验证"有没有人想玩"。

## 当前状态
- 单人模式：✅ 已完整，可玩
- 在线对战：❌ 没有服务器，只是空壳 UI
- data.js：33507 行，首次加载慢

## 待办任务（按顺序，约 2 小时）

### 任务 1：隐藏在线对战入口（15 分钟）
- 修改 `index.html`，隐藏"在线对战"卡片
- 修改 `js/game.js`，`renderMenu()` 里跳过在线相关初始化
- 不删除代码，只是隐藏——将来服务器写好了再打开

### 任务 2：修复已知 bug（15 分钟）
- `js/game.js` 第 326-335 行：`leaveOnlineLobby()` 函数重复定义了两次，删掉一个
- 检查有没有其他明显问题

### 任务 3：拆分 data.js 优化加载（30 分钟）
- 当前：`js/data.js` 33507 行，`<script src="js/data.js">` 同步加载阻塞渲染
- 改为：`data/players.json` 纯 JSON，前端用 `fetch()` 异步加载
- 改 `js/game.js` 中用到 `PLAYERS` 全局变量的地方，从同步改为异步初始化
- 预期效果：首屏不再被 3 万行数据阻塞

### 任务 4：移动端体验优化（30 分钟）
- 检查 `css/style.css` 响应式布局
- 优化触屏输入（自动补全下拉在手机上的表现）
- 确保表格在小屏幕上可滑动
- 如果有必要，调整字号和间距

### 任务 5：部署到 GitHub Pages（30 分钟）
- 确认项目结构可以直接托管（纯静态文件）
- 帮用户初始化 git 仓库
- 写好部署说明，用户 push 到 GitHub 后开启 Pages 即可

## 原则
- 不引入框架和构建工具，保持简单
- 不删功能，隐藏即可，将来可以恢复
- 零成本部署
- 先试试水，有人玩再迭代
